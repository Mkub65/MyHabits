<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:vm="clr-namespace:FirstApp.ViewModels"
             x:Class="FirstApp.AddingHabitPlanPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:AddingHabitPlanVM x:Key="vm"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Add new habit plan" />
            <Entry x:Name="habitPlanName" Placeholder="Name of Habit Plan" Text="{Binding Source={StaticResource vm},Path=HabitPlanName}"/>
            <DatePicker x:Name="startDate" Date="{Binding Source={StaticResource vm}, Path=StartDate}" MinimumDate="2022,1,1"/>
            <DatePicker x:Name="endDate" Date="{Binding Source={StaticResource vm}, Path=EndDate}" MinimumDate="{Binding Source={x:Reference startDate}, Path=Date}"/>
            <Button x:Name="addHabitPlan" Text="Add Habit Plan" Command="{Binding Source={StaticResource vm}, Path=AddHabitPlanCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>