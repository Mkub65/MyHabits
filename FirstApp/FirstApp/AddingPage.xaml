<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FirstApp.ViewModels"
             x:Class="FirstApp.AddingPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:AddingPageVM x:Key="vm"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Add New Habit" />
            <Entry x:Name="name" Placeholder="Name of Habit" Text="{Binding Source={StaticResource vm},Path=Name}" />
            <Entry x:Name="description" Placeholder="Description of Habit" Text="{Binding Source={StaticResource vm}, Path=Description}"/>
            <Button TextColor="Black" Text="Add Habit" Command="{Binding Source={StaticResource vm}, Path=AddHabitCommand}"/> 
        </StackLayout>
    </ContentPage.Content>
</ContentPage>